<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  	<ul>
  		<li>
  			<% if (user) { %>
  			<a href="/logout">Log Out: <%= user.name %> (<%= user.email %>)</a>
  			<% } else { %>
  			<a href="/auth/google">Login with Google</a>
  			<% } %>
  		</li>
  	</ul>
    <h1><%= title %></h1>
  </body>
</html>
