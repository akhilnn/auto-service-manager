<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body class="main-landing-page bg-secondary">
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark">
      <span class="navbar-brand">ASM v1</span>
      <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <% if (user) { %>
            <a href="/vehicles" class="nav-item nav-link">My Vehicles</a>
            <a href="/dealerships/new" class="nav-item nav-link">Add Dealership</a>
            <h5><a href="/logout" class="nav-item badge badge-success">Log Out: <%= user.name %> (<%= user.email %>)</a></h5>
            <% } else { %>
            <h5><a href="/auth/google" class="nav-item badge badge-light">Login with Google</a></h5>
            <% } %>
        </div>
      </div>
    </nav>
    <main class="text-center border border-white rounded-lg">
      <div class="container d-flex flex-column">
          <br />
          <h1 class="text-white">Welcome to ASM: <%= title %></h1>
          <br />
          <h6 class="text-white">Routine maintenance and important recalls are vital to keeping your vehicle in good shape, so use this application to track basic information about your vehicle and its service history!</h6>
          <br />
          <p class="text-white">Designed and Built by A. Neti, 2019</p>
          <br />
      </div>
    </main>
  </body>
</html>

